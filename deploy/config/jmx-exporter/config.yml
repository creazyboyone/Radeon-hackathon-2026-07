# JMX Exporter configuration — expose all JMX metrics in Prometheus format
# Used by jmx_prometheus_javaagent for all Hadoop/HBase/ZK/Hive daemons

rules:
  # Keep all default JMX attributes
  - pattern: '.*'
