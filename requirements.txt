# Pin setuptools <80: supervisorctl 4.2.5 depends on pkg_resources,
# which was removed in setuptools 80+. torch pulls in setuptools>=77.0.3,
# so without this pin pip would install setuptools 83.0.0 and break supervisorctl.
setuptools>=68,<80
requests>=2.31
fastapi>=0.104
uvicorn>=0.24
websockets>=12
wsproto>=1.2
# M5 知识库向量检索 (可选, 缺失自动降级为 BM25)
# 安装后首次使用会自动下载 bge-small-zh 模型 (~100MB)
sentence-transformers>=2.7
